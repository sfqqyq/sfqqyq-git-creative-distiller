services:
  backend:
    build: ./backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./storage:/app/storage
      - ./skills:/skills

  frontend:
    build: ./frontend
    ports:
      - "8788:80"
    depends_on:
      - backend
